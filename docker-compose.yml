version: '3.8'

services:
  analysis:
    container_name: polis-analysis
    image: polis-analysis-docker:local
    build:
      context: .
    ports:
      - "${JUPYTER_PORT:-8888}:8888"
    volumes:
      - ./notebooks:/usr/src/app/notebooks
      - ./data:/usr/src/app/data
